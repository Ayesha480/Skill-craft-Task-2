<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logo Design</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #000;
        }

        .logo-container {
            position: relative;
            display: inline-block;
            text-align: center;
            background-color: #333;
            border-radius: 50%;
            width: 250px;
            height: 250px;
            padding: 20px;
        }

        .logo-container:before {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            border: 5px dashed #000;
            border-radius: 50%;
        }

        .initials {
            font-family: 'Georgia', serif;
            font-size: 72px;
            color: white;
            letter-spacing: -5px;
            position: relative;
        }

        .swirl {
            font-family: 'Brush Script MT', cursive;
            font-size: 40px;
            color: #d4af37;
            position: absolute;
            top: 50px;
            left: 65px;
        }

        .company-name {
            font-family: 'Arial', sans-serif;
            font-size: 14px;
            color: white;
            text-transform: uppercase;
            margin-top: 15px;
        }

        .tagline {
            font-family: 'Arial', sans-serif;
            font-size: 10px;
            color: #d4af37;
            letter-spacing: 1px;
            text-transform: uppercase;
        }
    </style>
</head>
<body>
    <div class="logo-container">
        <div class="initials">AA</div>
        <div class="swirl">~</div>
        <div class="company-name">Areeba Abeer</div>
        <div class="tagline">Creative Company</div>
    </div>
</body>
</html>
